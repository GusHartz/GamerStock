## Packages
framer-motion | Essential for high-quality, professional page transitions and layout animations in the trading interface.

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
  mono: ["var(--font-mono)"],
}

The UI is forced into a dark-mode, high-contrast trading terminal aesthetic.
Ensure `useAuth` correctly handles Replit authentication redirects.
